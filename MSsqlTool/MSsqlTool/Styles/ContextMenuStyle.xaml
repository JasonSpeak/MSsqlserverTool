<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ContextMenu x:Key="MainMenu" DataContext="{Binding Source={StaticResource Locator}, Path=Main}">
        <MenuItem Header="导入" Command="{Binding ItemCommand}"/>
    </ContextMenu>
    <ContextMenu x:Key="DataBaseMenu" DataContext="{Binding Source={StaticResource Locator}, Path=Main}">
        <MenuItem Header="备份" Command="{Binding ItemCommand}"/>
        <ContextMenu.CommandBindings>
            <CommandBinding Command=""></CommandBinding>
        </ContextMenu.CommandBindings>
    </ContextMenu>
</ResourceDictionary>